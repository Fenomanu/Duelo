public enum WC_RES
{
    SWITCHING = 0,
    OBSTRUCTION = 1,
    TIMEOUT = 2
}
public enum HUI_ANIM
{
    PRESS_B = 3,
    PRESS_GRAB = 4,
    PRESS_TRIGG = 5,
}
public enum OPS
{
    ADD,
    SUB
}
public enum SIDE
{
    LEFT,
    RIGHT
}
public enum DIR
{
    UP,
    DOWN,
    RIGHT,
    LEFT
}
